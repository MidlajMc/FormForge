# FormForge
FormForge is a SwiftUI-based dynamic form engine that generates forms from JSON at runtime. It supports text fields, checkboxes, toggles, single and multi-select dropdowns, built-in validation, custom theming, and MVVM architecture. Designed for scalable, configurable, and server-driven form experiences with comprehensive test coverage.
